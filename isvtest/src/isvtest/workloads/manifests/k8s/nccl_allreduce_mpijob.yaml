# SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
# SPDX-License-Identifier: LicenseRef-NvidiaProprietary

# NVIDIA CORPORATION, its affiliates and licensors retain all intellectual
# property and proprietary rights in and to this material, related
# documentation and any modifications thereto. Any use, reproduction,
# disclosure or distribution of this material and related documentation
# without an express license agreement from NVIDIA CORPORATION or
# its affiliates is strictly prohibited.

apiVersion: kubeflow.org/v2beta1
kind: MPIJob
metadata:
  name: nccl-allreduce-multinode
spec:
  slotsPerWorker: 4
  launcherCreationPolicy: WaitForWorkersReady
  runPolicy:
    cleanPodPolicy: Running
  sshAuthMountPath: /root/.ssh
  mpiReplicaSpecs:
    Launcher:
      replicas: 1
      template:
        spec:
          containers:
            - name: launcher
              image: nvcr.io/nvidia/hpc-benchmarks:25.04
              command:
                - /bin/bash
                - -c
                - |
                  # MPI Operator mounts SSH keys read-only; copy to fix permissions
                  cp /root/.ssh/id_rsa /tmp/id_rsa && chmod 600 /tmp/id_rsa
                  # Note: '-np 8' is overridden at runtime by K8sNcclMultiNodeWorkload
                  exec mpirun --allow-run-as-root \
                    -np 8 \
                    --bind-to none --map-by slot \
                    -mca plm_rsh_args "-o StrictHostKeyChecking=no -o IdentityFile=/tmp/id_rsa" \
                    -x NCCL_DEBUG=INFO -x LD_LIBRARY_PATH \
                    -x NCCL_IB_DISABLE -x NCCL_IB_HCA -x NCCL_IB_GID_INDEX -x NCCL_NET_GDR_LEVEL \
                    -x UCX_TLS=tcp,sm,cuda_copy,cuda_ipc \
                    /usr/local/bin/all_reduce_perf_mpi -b 8 -e 4G -f 2 -g 1
              env:
                - name: NCCL_IB_DISABLE
                  value: "0"
                - name: NCCL_IB_HCA
                  value: "^rocep75s0"  # exclude broken NIC (duplicate .16 IP on both nodes)
                - name: NCCL_IB_GID_INDEX
                  value: "3"           # RoCE v2 — valid unique IPs on the remaining NICs
                - name: NCCL_NET_GDR_LEVEL
                  value: "0"           # host-bounce buffers; set to 2 only if nvidia_peermem is loaded
          tolerations:
            - key: "node-role.kubernetes.io/control-plane"
              operator: "Exists"
              effect: "NoSchedule"
          affinity:
            nodeAffinity:
              requiredDuringSchedulingIgnoredDuringExecution:
                nodeSelectorTerms:
                  - matchExpressions:
                      - key: node-role.kubernetes.io/control-plane
                        operator: Exists
    Worker:
      replicas: 2
      template:
        spec:
          containers:
            - name: worker
              image: nvcr.io/nvidia/hpc-benchmarks:25.04
              command:
                - /bin/bash
                - -c
                - "command -v sshd >/dev/null || { apt-get update -qq && apt-get install -y -qq openssh-server >/dev/null 2>&1; }; mkdir -p /var/run/sshd && ssh-keygen -A 2>/dev/null && exec /usr/sbin/sshd -De -o PermitRootLogin=yes -o StrictModes=no"
              readinessProbe:
                tcpSocket:
                  port: 22
                initialDelaySeconds: 2
                periodSeconds: 3
              env:
                - name: NCCL_IB_DISABLE
                  value: "0"
                - name: NCCL_IB_HCA
                  value: "^rocep75s0"  # exclude broken NIC (duplicate .16 IP on both nodes)
                - name: NCCL_IB_GID_INDEX
                  value: "3"           # RoCE v2 — valid unique IPs on the remaining NICs
                - name: NCCL_NET_GDR_LEVEL
                  value: "0"           # host-bounce buffers; set to 2 only if nvidia_peermem is loaded
              securityContext:
                privileged: true       # required to open /dev/infiniband/uverbs* (cgroup device whitelist)
              resources:
                limits:
                  nvidia.com/gpu: 4 # overridden at runtime by K8sNcclMultiNodeWorkload
              volumeMounts:
                - name: shm
                  mountPath: /dev/shm
                - name: infiniband
                  mountPath: /dev/infiniband
          nodeSelector:
            nvidia.com/gpu.present: "true"
          runtimeClassName: nvidia
          tolerations:
            - key: "nvidia.com/gpu"
              operator: "Exists"
              effect: "NoSchedule"
          volumes:
            - name: infiniband
              hostPath:
                path: /dev/infiniband  # exposes host RoCE/IB verbs devices without a device plugin
            - name: shm
              emptyDir:
                medium: Memory
                sizeLimit: 8Gi
