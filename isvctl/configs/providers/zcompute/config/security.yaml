# zCompute Security Validation Configuration
#
# Only steps for RELEASED validations are included.
# Unreleased validations are skipped by the framework automatically and their
# steps are omitted here to keep the pipeline clean.
#
# Released checks (as of isvtest 0.6.8):
#   SEC14-01: ApiEndpointIsolationCheck  — API not publicly accessible
#   SEC07-01: MfaEnforcedCheck           — MFA enforced on admin interfaces
#   SEC09-04: CustomerManagedKeyCheck    — BYOK/CMK encryption support
#   SEC03-01: ServiceAccountCredentialCheck — service account IAM credentials
#   SEC01-01: OidcUserAuthCheck          — OIDC token validation
#
# Excluded (VM cloud — no BMC/IPMI/Redfish hardware management layer):
#   BmcManagementNetworkCheck, BmcTenantIsolationCheck,
#   BmcProtocolSecurityCheck, BmcBastionAccessCheck
#
# Usage:
#   export ZCOMPUTE_BASE_URL=https://172.29.0.20
#   export AWS_ACCESS_KEY_ID=<key>
#   export AWS_SECRET_ACCESS_KEY=<secret>
#   export AWS_REGION=symphony
#
#   uv run isvctl test run -f isvctl/configs/providers/zcompute/config/security.yaml

import:
  - ../../../suites/security.yaml

version: "1.0"

commands:
  security:
    phases: ["setup", "test", "teardown"]
    steps:
      - name: preflight
        phase: setup
        command: "echo"
        args: ["{\"success\": true, \"platform\": \"security\", \"test_name\": \"preflight\"}"]
        timeout: 10

      - name: api_endpoint_isolation
        phase: test
        command: "python3 ../scripts/security/api_endpoint_test.py"
        args: ["--region", "{{region}}"]
        timeout: 60

      - name: mfa_enforcement
        phase: test
        command: "python3 ../scripts/security/mfa_enforcement_test.py"
        args: ["--region", "{{region}}"]
        timeout: 60

      - name: customer_managed_key_test
        phase: test
        command: "python3 ../scripts/security/customer_managed_key_test.py"
        args: ["--region", "{{region}}"]
        timeout: 60

      - name: sa_credential_test
        phase: test
        command: "python3 ../scripts/security/sa_credential_test.py"
        args: ["--region", "{{region}}"]
        timeout: 120

      - name: oidc_user_auth_test
        phase: test
        command: "python3 ../scripts/security/oidc_user_auth_test.py"
        args: ["--region", "{{region}}"]
        timeout: 60

      - name: teardown
        phase: teardown
        command: "python3 ../scripts/security/teardown.py"
        args: ["--region", "{{region}}"]
        timeout: 120

tests:
  cluster_name: "zcompute-security-validation"
  description: "zCompute security hardening validation (VM cloud — BMC checks excluded)"

  settings:
    region: "symphony"

  exclude:
    tests:
      # zCompute is a VM cloud — no BMC/IPMI/Redfish hardware management layer
      - BmcManagementNetworkCheck
      - BmcTenantIsolationCheck
      - BmcProtocolSecurityCheck
      - BmcBastionAccessCheck
